pipeline {
    agent any
    
    stages {
        stage('Build'){
    		steps {
		        git ''
		        bat '.mvnw clean compile'
		    }
           
        }
        stage('Test') {
           steps {
               bat '.mvn test'
           }
           post {
               always {
                   junit '**/target/surefire-reports/TEST-*.xml'
               }
           }
        }
    }
}