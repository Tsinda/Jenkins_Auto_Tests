pipeline {
    agent any
    
    stages {
        stage('Build'){
    		steps {
		        git 'https://github.com/Tsinda/Jenkins_Auto_Tests.git'
		        bat '.mvnw clean compile'
		    }
           
        }
        stage('Test') {
           steps {
               bat '.mvn test'
           }
           post {
               always {
                   junit '**/target/surefire-reports/TEST-*.xml'
               }
           }
        }
    }
}